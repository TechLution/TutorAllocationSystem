{
  "scripts": {
    "test": "jest",
    "coverage": " jest --coverage",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
